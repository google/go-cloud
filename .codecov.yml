codecov:
  # Set via secrets, see .github/workflows/tests.yml.
  token: ""
coverage:
  status:
    project:
      default:
        target: 0
        threshold: null
        base: auto
    patch:
      default:
        target: 0
        threshold: null
        base: auto
